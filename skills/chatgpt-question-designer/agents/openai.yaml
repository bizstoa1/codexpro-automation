interface:
  display_name: "ChatGPT Question Designer"
  short_description: "Design Oracle+DevSpace missions, including explicit read/write Pro routes."
  default_prompt: "Design a purpose-specific GPT mission using Oracle+DevSpace; default to the highest-tier non-Pro route and select read/write qualified Pro only after explicit user opt-in. Use `pro-attachment` only for DevSpace-unreadable immutable evidence. Never route new work through CodexPro."
